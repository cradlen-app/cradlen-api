/**
 * Currency constants for the financial layer.
 *
 * The schema defaults every `currency` column to EGP; this is the single
 * source for that default so services and DTOs stop hard-coding the string.
 */

export const DEFAULT_CURRENCY = 'EGP';
